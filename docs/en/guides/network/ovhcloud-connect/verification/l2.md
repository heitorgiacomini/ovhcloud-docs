{/* Internal verification report — not a published guide. Do not add to the sidebar / build. */}

# Verification — l2.mdx

- **Previous-doc source:** ovhcloud-connect-old/occ-layer2.mdx (concept + implementation) + occ-direct-control-panel.mdx (CP L2 steps) + api.mdx (API L2 config)
- **Sources checked:** previous docs, SOT cache (api / manager-beta / terraform / docs), OCC contract, ovhcloud.com
- **Method:** /fact-check SOT-veracity + previous-doc comparison
- **Note:** This guide is a **restoration** of the dropped "Layer 2 mode" page — almost all content is carried over from the previous docs.

## 1. Already present in the previous docs & verifiable
| Claim | Line | Evidence |
|---|---|---|
| L2 links your network at the data-link layer; transparent to VLANs (802.1q); no routing | 11, 32-34 | occ-layer2 lines 31, 34 ("transparent to VLANs", "no routing involved") |
| The virtual link is an L2 tunnel forwarded to/from the vRack | 22 | occ-layer2 line 17 |
| Broadcast forwarded; unknown unicast forwarded; multicast forwarded as broadcast (20 pps); LLDP/LACP not forwarded | 30-33 | occ-layer2 lines 21-24 |
| VLANs preserved end to end (same IDs in your network and the vRack) | 35 | occ-layer2 line 34 + faq "VLANs are the same in your DC and OVHcloud" |
| Point-to-point: one PoP to one AZ; no second-PoP backup | 13, 41 | occ-layer2 lines 26, 30, 38 |
| Only one OVHcloud Connect L2 per vRack | 13, 42 | occ-layer2 line 26 |
| Redundancy only via LACP on the same PoP; LACP mandatory for two links | 43 | occ-layer2 lines 30, 42 |
| Jumbo frames up to 9000 bytes | 44 | occ-layer2 line 44; occ-limits line 18 |
| CP: Bare Metal Cloud > Network > OVHcloud Connect; Attach a vRack; **Configure port** → "Add a PoP configuration" (Type **L2**); **Add an AZ** | 57-63 | occ-direct-control-panel lines 27, 33, 57, 91-99 + **live-verified (authenticated CP)**: labels corrected to "Configure port"/"Add an AZ" (were "Add a PoP configuration" button / "Add a configuration"). Guide corrected this pass. |
| Switching L2<->L3 requires deleting the whole configuration | 47 (callout) | occ-direct-control-panel line 44; api.mdx line 49 |
| API: GET interface; POST config/pop (type l2); POST config/pop/{popId}/datacenter (datacenterId) | 66-84 | api.mdx lines 57, 69-77, 109-116; all routes exist in SOT api.json |
| Auto-negotiation not supported; disable it and force port speed | 90 | occ-limits line 20; troubleshooting lines 116-135 |
| L2 diagnostic = MAC address list | 100 | occ-diagnostics lines 27-28, 67-71 |
| Diagrams (implementation, traffic, supported/unsupported, topologies) | 20, 37, 51-53 | re-used from occ-layer2/images (occ-l2-implementation/trafic/supported-unsupported/topologies) |

## 2. New (not in the previous docs) but verifiable
| Claim | Line | Evidence |
|---|---|---|
| L2 is available on OVHcloud Connect Direct only (Provider is Layer 3 only) | 13 | occ-provider-control-panel line 52 ("requires an L3 level configuration"); ovhcloud.com (Provider = L3); introduction L2/L3 table |

## 3. New & not verifiable without testing
_None — the guide is a faithful restoration of the previous "Layer 2 mode" documentation; every claim is grounded in the previous docs or the SOT/contract/website._

## Summary
- Category 1: 13 · Category 2: 1 · Category 3: 0
- Notes: `l2.mdx` restores the dropped L2 page. Concept, traffic-handling, topology/redundancy, CP steps, API calls and the four capability diagrams all carry over from occ-layer2 / occ-direct-control-panel / api.mdx. The only "new" item is the explicit Direct-only framing, which is verifiable. No unverifiable content.
